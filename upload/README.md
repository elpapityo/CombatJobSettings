# upload

公開する動作確認済みZIPをこのフォルダーへ置きます。

ファイル名:
`CombatJobSettings_vX.Y.Z.zip`

例:
`CombatJobSettings_v0.1.1.zip`

ZIPをアップロードした後、GitHub Actions の `Publish Combat Job Settings upload` を手動実行すると、ZIP内容とバージョンを確認し、pluginmaster.json を同期して Release を作成または差し替えます。
